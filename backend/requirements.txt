fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
# >=2.7 for NoDecode, which lets CORS_ORIGINS be a comma-separated env var (app/config.py)
pydantic-settings>=2.7
# MCP server exposing the scenario engine to agents (app/api/mcp_tools.py)
mcp>=2.2
# NIM chat client for the Nemotron analyst and reviewer (app/agent/nemotron.py); mcp already depends on it
httpx2>=2.5
# Eclipse SUMO: simulator binaries + Python TraCI/sumolib. No GPU required.
eclipse-sumo>=1.20
traci>=1.20
sumolib>=1.20
# sumolib's x/y <-> lon/lat for geo-referenced (OSM-built) networks
pyproj>=3.6
