# Copy to .env at the repository root. Only API keys are read from the environment.
# Maps, simulation speed, scripts, memory mode and analyst selection are runtime controls.
# Model ids and safe demo behavior are code defaults.

# ANTHROPIC_API_KEY=your-anthropic-api-key
# NVIDIA_API_KEY=your-nvidia-api-key
