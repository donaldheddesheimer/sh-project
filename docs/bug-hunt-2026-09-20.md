# Bug hunt: the deployed console, 2026-09-20

| | |
|---|---|
| **Target** | https://traffic-ops-demo-424216851088.us-central1.run.app (Google Cloud Run, one instance; not Vercel) |
| **Tested** | 2026-09-20, about 07:15Z to 10:50Z, from Windows with curl 8.21 over HTTPS and WSS |
| **Code reviewed** | local `feature/console-decision-layout` at 55e281a, plus the 9 newer commits on `origin/feature/console-decision-layout` (read with `git show`; no checkout) |
| **Method** | Manual requests against the deployed URL (REST, one WebSocket capture, static files), two background samplers polling `/api/state` once a minute, and reading the frontend and the backend paths behind what looked wrong |
| **Not used** | A browser. No browser-control tool was available, so **nothing here comes from clicking through the console**. No tests, lint or build were run (CLAUDE.md rule 1) |

This is a dated record. The build it tested was already behind the branch (section 0), so re-run the checks in section 8 after the next deploy.

## Evidence levels

- **LIVE**: reproduced against the deployed URL.
- **CODE**: read from source, not executed. The visible symptom still needs a browser (section 7).

Line numbers are from local HEAD 55e281a; `origin` has shifted a few. UI regions use the README's names (Command bar, Workspace rail, Workspace drawer, Performance dock).

## Summary

49 findings: 4 demo-critical, 16 on the deployed instance, 5 on delivery and performance, 20 in the frontend, 4 in config and docs.

1. **D1** The city empties 24 simulated hours after a reset or boot: 90 minutes of wall time at 16x.
2. **D2** Traffic degrades into gridlock about 2.5 simulated hours after a reset: about 9 minutes at 16x.
3. **D3** `/mcp` returns 421 on the public host, although the README says the Cloud Run URL carries it.
4. **D4** Analyze takes about 2 minutes on the container and the live clock drops to 6-9x while it runs.
5. **Section 0** The deployed build predates the Analyze overlay, Agent decision card, Response routes, splitters and the always-armed startup.
6. **L1, L2** The recommendation can pick a corridor plan whose corridor did nothing, and it can worsen max queue while saying "less queue".
7. **L4, L7** Clear scene clears every crash on the segment; a run aborted by a reset keeps candidates stuck at `running`.
8. **F1, F2, F3, F5** Maps rebuild on every WebSocket reconnect; "change vs pre-incident" vanishes after 15 simulated minutes; clearing the scene erases the analysis; the newest commit made the splitters unreachable by keyboard.
9. **P1, P4** Nothing is compressed on the wire, and the state stream is about 1.2 MB/s uncompressed at 16x.

## 0. What is deployed (LIVE)

The deployed build is from about 01:17 EDT, before PR #22 and everything after it.

| Evidence | Value |
|---|---|
| `index.html` last-modified | Sun, 20 Sep 2026 05:17:37 GMT (01:17 EDT) |
| `/openapi.json` demo routes | `/api/demo`, `/api/demo/start`, `/api/demo/stop`, **`/api/demo/analyst`**; no `/api/demo/analyze` |
| `/api/health` | `agent: mock` (branch default: `nemotron`) |
| `/api/demo` | `armed: null` at boot (branch arms `operator-collision` at startup); `analyst: nemotron` |
| JS bundle strings | has "Analyze Response"; has no "Agent decision", "Response routes" or "Reset layout" |
| Route list | identical to commits 05503d5 and 8df6219 |

Consequences:

- The Analyze overlay, Agent decision card, Response routes mini map, splitters, always-armed startup, 1x analysis hold and the thinking overlay do not exist at this URL. A redeploy is needed to test the current code.
- The running build reports memory at `/tmp/traffic-memory` and a start speed of 16x. Neither is a branch default (`<repo>/memory`, 4x) or set in the Dockerfile, so they probably come from Cloud Run env vars, which the branch ignores (API keys only). A redeploy will change both silently. This is an inference: the service config was not readable.

## 1. Demo-critical

| ID | Finding | Evidence |
|---|---|---|
| **D1** | **The city goes empty 24 simulated hours after a reset or boot**, which is **90 minutes of wall time at 16x** (6 hours at 4x). I found the twin at sim 143,302 s (39.8 h) with `vehicles: 0`, `throughput: 0`, all 332 segments empty and signals still cycling, while 2 clients were connected. Inject and Analyze on an empty city are meaningless. | LIVE. Every flow in both demand files is `begin="0" end="86400"`: 393 in `simulation/scenarios/pittsburgh_oakland/demand.rou.xml`, 132 in `downtown_grid/demand.rou.xml`. `scenario.sumocfg` sets no `<end>`. |
| **D2** | **Traffic degrades into gridlock about 2.5 simulated hours after a reset** (about 9 minutes at 16x). Mean delay is 34-71 s until 2.4 h, then 100 s (2.7 h), 142 s (3.3 h), 287 s (3.9 h), 505 s (4.4 h), 782 s (5.5 h), 1,209 s (7.6 h). Vehicles go from 130-170 to 350-450, stopped vehicles from 20-40 to 200-280, and the insertion backlog reaches 16-46 by 7.6 h. The first probe of the idle instance found delay 933 s at 5.7 h. The README's "stable for two simulated hours" is true and stops where an idle demo begins. | LIVE, two samplers, one sample a minute (Appendix A). |
| **D3** | **`/mcp` is unusable on the deployed URL.** `POST /mcp` returns **421 "Invalid Host header"**; `POST /mcp/` returns 405. The README says the Cloud Run URL carries the MCP endpoint. This confirms, on the real host, the `/mcp` 421 item already in `TOOL_TODO_FIX.txt` (MCP SDK DNS-rebinding protection allows only local hosts). | LIVE. |
| **D4** | **Analyze takes about 2 minutes on the container**: 117 s on the jammed twin and 110 s on a clean one (8 plans, 4 branches at a time, 46-83 s each), against the README's 25-60 s. **The live twin runs at only 6-9x instead of the configured 16x** while it works. The plan was **2,056 simulated seconds stale** when implemented (about 34 minutes; part of that was operator delay). The deployed build did not hold the clock at 1x. Container size is unknown (the README says 2 vCPU, `scripts/deploy-cloudrun.sh` says 4). | LIVE. |

## 2. Findings on the deployed instance (LIVE)

| ID | Sev | Finding | Evidence and where |
|---|---|---|---|
| L1 | Med | **The recommendation picks a corridor plan whose corridor did nothing.** Clean-city run: `corridor-plus-divert` recommended; its notes say "no pre-emptions", and `divert-advisory` has identical numbers (delay 109.3, queue 54, throughput 2,448, EMS 54 s). The tie is broken by proposal order, and the summary still says "Green corridor for the responder ... less queue". | `backend/app/agent/mock.py:365` (`min(pool, key=mean_vehicle_delay)` keeps the first listed on ties), `:372`. Prefer the simpler plan on ties and ignore components that did nothing. |
| L2 | Med | **The recommended plan worsens max queue, and its delay gain is under the app's own 5% materiality bar.** Clean run: delay 115 -> 109 s (-5%), **max queue 51 -> 54**, EMS 0:54 -> 0:54. Jammed run: -2% delay still recommended, EMS null in every branch (the probe stood still 586 of 600 s). The rubric looks at delay only. | `agent/mock.py:333-372`; thresholds in `learning/scorecard.py`. |
| L3 | Med | **EMS ETA saturates instead of growing.** A responder stopped at speed 0 for about 3,500 simulated seconds showed ETA 327 s, then 355 s; the ops log later recorded a 59:03 response. The estimator floors the responder's own speed at 0.3 m/s, so the "growing ETA" its docstring promises never happens. | `backend/app/simulation/sumo.py:918` (`speed = max(0.3, ...)`). |
| L4 | Med | **Clear scene clears every crash on the segment, not just the incident's.** Two default injects gave INC-0001 and INC-0002 (same segment, lanes and position). Clearing INC-0001 returned `cleared_disruptions: ["C-0ab301","C-2649b0"]` and INC-0002 was closed too, with no operator action. | `backend/app/services/city.py:331-345` filters by `d.segment_id`. Match by the incident's own disruption ids. |
| L5 | Low | Clearing an already-cleared incident returns 200 and logs a second "INC-0001: scene cleared, lanes reopened". | `city.py:344` logs unconditionally. |
| L6 | Low | Inject collision is not idempotent: two clicks stack two incidents and six phantom vehicles at the same spot. | LIVE. |
| L7 | Med | **A failed or aborted run keeps its candidates `running` and `pending` forever.** Reset during an analysis gave `status: failed, error: "the simulation was reset"`, yet 40 s later the candidates were still `running=4 pending=3` (the UI renders spinners and "Waiting for a simulation worker" on a failed run). | `backend/app/services/scenarios.py` `fail()` (origin line 438), `abandon()` (589) and `_close()` (600) never touch candidate statuses. |
| L8 | Low | **`POST /api/simulation/reset` returns the previous run's `sim_time`** (`36821.5` while the new state was `332.5`; `146166.5` vs `350.0`). Reset takes about 6 s idle and **22 s** with an analysis in flight. | `city.py:286`, `routes.py` `_control()` reads the last published frame. |
| L9 | Low | `GET /api/events?limit=0` returns the **whole** log; negative limits drop from the front; there are no bounds. | `backend/app/services/events.py:35` (`list(...)[-limit:]`). Use `Query(ge=1, le=N)`. |
| L10 | Low | **A wrong-method request to a real route returns 404, not 405** (`GET /api/simulation/reset`, `/simulation/start`, `/demo/start`). Likely because the `StaticFiles` mount at `/` wins over Starlette's partial match. | `backend/app/main.py:47` (mounted at `/`, after the routers). |
| L11 | Low | The incident text calls the **middle lane of a 3-lane road the "left lane"**: "blocking right lane, left lane" for `affected_lanes [0,1]` with `total_lanes 3`, while the lane diagram in the same card shows the two right lanes blocked. Memory names lanes correctly, so live text and stored lessons disagree. | `backend/app/smart_city/mock.py:18,93` (unchanged on origin); correct logic in `learning/store.py:76`. |
| L12 | Low | `POST /api/emergency/dispatch` has no cap and accepts origin == destination (a responder spawned on the crash segment). Each call adds a vehicle. | LIVE. |
| L13 | Low | The API leaks the server path (`/api/demo` and `/api/memory` show `/tmp/traffic-memory`). Destructive endpoints (`reset`, `map`, `DELETE /api/memory`, `demo/analyst`, `/mcp`) need no auth: documented, but the URL is public. | LIVE. |
| L14 | Low | The idle twin runs at 16x around the clock with nobody watching (cost), and drifts into D2 and D1. | LIVE. |
| L15 | Low | A POST with no body needs `Content-Length: 0`: Google's front end answers curl's bodyless POST with an HTML "411 Length Required". Browsers are fine, but curl recipes in the docs should send `-d ''` or `-H "Content-Length: 0"`. | LIVE (a probe artifact worth a doc line). |
| L16 | Low | Live "Mean vehicle delay" (257 s in my run) and the analysis "Mean delay" (115 s) are different quantities under near-identical labels in the same console. The README notes the difference; the UI does not. | LIVE. |

## 3. Delivery and performance (LIVE)

| ID | Sev | Finding |
|---|---|---|
| P1 | Med | **Nothing is compressed on the wire**: JS 1,335,689 B, map worker 508,762 B, geojson 498,482 B, CSS 128,665 B, `/api/state` 175,017 B, `/openapi.json` 59,241 B, none with `Content-Encoding`. First load is about 2.6 MB plus the 363 KB WebSocket `hello`. Add `GZipMiddleware` or precompress. |
| P2 | Low | `Cache-Control: private` and `Expires: <now>` on every response, including hashed `/assets/*`, and a `GAESA` affinity cookie on static files. Session affinity does nothing with `--max-instances 1` and forces revalidation. |
| P3 | Low | `/oakland-context.geojson` is served as `application/octet-stream`. |
| P4 | Med | **The state stream is heavy.** At 16x the server sends a full `CityState` (about 172 KB) every 2.2 simulated seconds: **about 7 frames a second, about 1.2 MB/s**, measured uncompressed over 8 s (55 frames, 9.83 MB). Static fields (names, ids) are resent every frame. The server accepts `permessage-deflate`, so browsers should see less; that was not measured. |
| P5 | Low | No security headers on this origin (`X-Content-Type-Options`, frame protection or CSP, `Referrer-Policy`, `Permissions-Policy`). They exist only in `vercel.json`, which is not what is deployed. |

## 4. Frontend findings (CODE, current branch)

| ID | Sev | Finding | Where |
|---|---|---|---|
| F1 | Med | **Both maps are destroyed and rebuilt on every WebSocket reconnect.** Each `hello` carries a new `network` object, and `CityMap` and `MiniMap` key their effect on `[network]` (identity). Cloud Run closes sockets at 60 minutes and laptops sleep or blip: pan and zoom, the plan overlay and markers reset, and the 498 KB geojson is fetched twice. Key on `network.id`. | `CityMap.tsx:102-193`, `MiniMap.tsx:72-129`, `useCityStream.ts:125` |
| F2 | Med | **"Change vs pre-incident" in the Performance dock disappears 15 simulated minutes after detection** (56 s of wall time at 16x). The reference is the last trend sample before the incident, but history keeps only 180 samples (15 simulated minutes). A 2-minute analysis at 16x outlasts it. | `App.tsx:135-138`, `useCityStream.ts:20`, `services/city.py:61` |
| F3 | Med | **Clearing the scene erases the analysis from every panel**, including an in-flight run's progress, the recommendation, Scenario comparison and "Applied to live signals". `scopedScenario` requires an active incident. The backend keeps the run: SCN-0003 completed normally after I cleared its scene. | `App.tsx:129-132` |
| F4 | Med | **Stale instruction.** The Response plans empty state says "Inject a collision, then click Analyze Response", but that button exists only in fixture mode. The real trigger is the Analyze overlay, shown only while an episode is `awaiting`. With no armed script (for example after Stop) the UI has no way to start an analysis. | `ResponsePlans.tsx:80`, `TopBar.tsx:164` |
| F5 | Med | **Keyboard regression on origin.** The ARIA commit (af5ac1c) removed `tabIndex={0}` from `Splitter`, so no splitter can take focus and `onKeyDown` is dead code, while README line 268 promises arrow-key resizing. It also dropped the hover `title` help text, and now calls `getBoundingClientRect()` during render to compute `aria-valuenow`. | `Splitter.tsx` on `origin/feature/console-decision-layout` |
| F6 | Med | **The palette has 7 colors for up to 8 non-baseline plans.** On the grid the mock proposes baseline plus 8 (including `flush-downstream`), so the last plan, `divert-advisory`, the usual winner there, gets the overflow gray `#5f6b7c` beside the baseline gray `#8f99a8`. | `lib/plans.ts:20-32`, `agent/mock.py:217` |
| F7 | Med | A failed or aborted run shows stuck "Running..." and "Waiting for a worker" cards (see L7). The UI could also treat non-terminal candidates in a `failed` run as cancelled. | `plans/CandidateCard.tsx:123-132` |
| F8 | Low | **The Workspace rail's Agent count includes an armed-but-idle script** (`armed` and `reviewing` are in the list), so it is lit permanently once `operator-collision` auto-arms. The README says the count is "a working episode", and `WORKING` in `plans.ts` excludes both. | `App.tsx:288` vs `lib/plans.ts:64` |
| F9 | Med | **No confirmation on destructive controls**: Reset (Command bar), Clear scene, Clear memory (a small text link), and **Arm/Run**, whose tooltip shows the script description instead of warning that it resets the city and aborts a working episode. | `TopBar.tsx:131`, `IncidentPanel.tsx:157`, `EpisodePanel.tsx:300-334` |
| F10 | Low | On Oakland, plan chips, map badges and run notes identify signals by raw OSM node ids (`105013312`), not street names. | `lib/plans.ts:138`, `plans/MapPlanCard.tsx`, `CityMap.tsx:298` |
| F11 | Low | `innerHTML` is used for OSM-derived street and station names (`markerElement`). Harmless with the bundled extract; an HTML-injection path if the extract is refreshed. | `CityMap.tsx:146-158` |
| F12 | Low | **Fixture replay only appears when the live incident is `INC-0001`** (hard-coded in the recording). Incident ids keep counting across a plain reset (I saw INC-0003 then INC-0004), so it also breaks after the first crash of a session. | `dev/replay.ts:19`, `dev/scenario-run.json:3` |
| F13 | Low | Cosmetics: TrendChart axis prints `0 / 1 / 1` when the series maximum is 0-1 (`Math.round(0.5)`); "EMS ETA **5:27 min**" mixes mm:ss with "min"; "Analyst nemotron" vs "Mock" capitalisation; the Agent decision card defaults its analyst label to "Nemotron" before any episode even if the analyst is mock; the sim clock wraps at 24 h with no day counter; the KPI cell says "not on scene" even for runs with no EMS probe. | `TrendChart.tsx:32,51`, `plans/AnalysisDock.tsx:125`, `EpisodePanel.tsx:316`, `DecisionCard.tsx:13,17`, `lib/format.ts:8`, `plans/KpiComparison.tsx:79` |
| F14 | Low | `useCityStream`: `JSON.parse` is unguarded; `status.error` is carried forward forever; retry is a fixed 1 s with no backoff or jitter; in dev StrictMode the first socket's `onclose` can leave `connected=false` while the second is open. | `useCityStream.ts:121,142,164` |
| F15 | Low | Saved panel sizes are not re-clamped on load (`--side-w`, `--dock-h` have no `min()`), so a size dragged on a big monitor can crush the map on a small window. | `hooks/usePanelSizes.ts`, `console.css:33-34` |
| F16 | Low | The speed `<select>` has no matching option (and no segment is active) when the server speed is outside {1, 2, 4, 8, 16}; the API accepts 0 < x <= 64. | `TopBar.tsx:6,101-113` |
| F17 | Low | `Section` unmounts its children when collapsed, so toggling "Response routes" destroys and recreates a WebGL map each time. | `Section.tsx:27`, `RoutePanel.tsx` |
| F18 | Low | Accessibility: `<h2>` inside `<button>`; ARIA table header cells outside a `row`, and the plan-name cell is not a `cell`; the Analyze dialog has no focus management or `aria-modal`; the toast is a clickable `div`; speed buttons lack `aria-pressed`; `aria-label` on non-interactive spans and divs. | `Section.tsx:20-24`, `KpiComparison.tsx:36`, `AnalyzeOverlay.tsx:30`, `App.tsx:430`, `TopBar.tsx:101` |
| F19 | Low | Performance risks (unmeasured): per state frame (about 7 Hz at 16x) roughly 500 `setFeatureState` calls, a rebuilt 368-feature vehicles source and EMS marker `innerHTML`; the thinking overlay rewrites a `line-gradient` per route at about 30 fps for the whole analysis. | `CityMap.tsx:209-254`, the thinking effect on origin |
| F20 | Low | Product note, not a defect: the thinking HUD rotates labels such as "Estimating flow" and "Checking corridor access" and draws its EMS approach in the same red as real EMS routes. It is disclaimed ("illustration · not the agent's plan") but reads as agent work. | `map/ThinkingCaption.tsx`, `lib/thinkingRoutes.ts` |

## 5. Config and docs

| ID | Sev | Finding |
|---|---|---|
| X1 | Med | **`vercel.json` cannot work with the current client.** It rewrites `/(.*)`, including `/api/*`, to `/index.html`; the client is hard-wired same-origin (`API_BASE_URL = ''`, `VITE_*` removed); and Vercel does not proxy WebSockets. A Vercel-hosted build would get HTML for every API call. The deployed (older) bundle still had an API-base override, so it could point at another host; the branch cannot. |
| X2 | Low | The README deploy command uses `--cpu=2`; `scripts/deploy-cloudrun.sh` uses `--cpu 4`, and its comment cites `SCENARIO_WORKERS=4`, an env var the app no longer reads. |
| X3 | Med | The README says the Cloud Run URL carries the MCP endpoint (see D3). |
| X4 | Low | The Smart City provider was renamed `mock` to `simulation` on origin; the README's Terms and Settings still say mock in places, and the deployed build still reports `mock`. |

## 6. Checked and fine

- Input validation is good: unknown ids give 404, bad JSON or types 422, speed bounds (0 < x <= 64), horizon 120-1800, lanes checked against the segment, unknown map 404, unknown demo script and analyst 404, analysis conflict 409, applying twice 409, dispatch with no incident 409.
- Two analyses at once return 409. After a reset the lock is released (a new run started immediately) and the live clock returned to 16.0x, so there is no sign of leaked branch processes.
- `aggressive-flush` is rejected with the expected `green 8s < 12s` violation.
- Map switch: the grid is ready in under 3 s and Oakland in about 6 s; scenarios and the ops log start fresh; both directions worked.
- The WebSocket works over `wss` and accepts `permessage-deflate`. The paused runner still sends a frame every second, so a page loaded while paused fills in within a second.
- Worker script MIME (`text/javascript`), favicon, `/docs`, `/openapi.json`, and a CORS preflight from a foreign origin (400) are fine. The bundle references no external tile or font hosts.
- Network geometry: no empty names, degenerate segments or duplicate ids.

## 7. Checks that need a browser

These were read from code and never seen. Each is a quick manual check once a person or a browser tool is available.

1. **F1**: with the console open, take the network offline and back online in DevTools (or restart the backend). Does the map flash and lose pan and zoom?
2. **F2**: at 16x, Inject collision, arm and run the analysis, then watch the Performance dock. Does "Change is vs pre-incident" disappear after about a minute?
3. **F3**: run an analysis, then Clear scene. Do the Agent decision card and Response plans lose it?
4. **F5**: on the branch head, Tab to a splitter and press the arrow keys.
5. **F6**: on the 3x3 Grid, run an analysis and compare the last plan's color with the baseline's in the horizon chart.
6. **F7**: Reset during an analysis. Do "Running" cards remain on a failed run?
7. **F4**: Stop the armed script and read the Response plans empty state.
8. **F15**: drag the drawer wide on a large monitor, then reopen in a small window.
9. **F16**: `POST /api/simulation/speed {"multiplier":3}` and read the Command bar's speed control.
10. **L11**: on Oakland, compare the incident text with the lane diagram.
11. **F18**: keyboard and screen-reader pass over the Analyze overlay and the dock tabs.
12. **F19, P4**: DevTools Performance during an analysis at 16x, and WebSocket frame sizes in the Network tab (does the browser negotiate `permessage-deflate`?).

## 8. Re-verifying the LIVE findings

These mutate the shared instance (inject, reset), so run them when nobody is presenting. In PowerShell:

```powershell
$url = "https://traffic-ops-demo-424216851088.us-central1.run.app"

# Which build is deployed?  (stale build: agent "mock", /api/demo/analyst, old last-modified)
curl.exe -s "$url/api/health"
curl.exe -s "$url/openapi.json" | Select-String -Pattern '/api/demo/analy(st|ze)' -AllMatches | ForEach-Object { $_.Matches.Value } | Sort-Object -Unique
curl.exe -s -D - -o NUL "$url/" | Select-String last-modified

# D3: MCP on the public host (stale build: "Invalid Host header [421]")
'{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-03-26","capabilities":{},"clientInfo":{"name":"probe","version":"0"}}}' | Set-Content -NoNewline -Encoding ascii init.json
curl.exe -s -w " [%{http_code}]`n" -X POST "$url/mcp" -H "Content-Type: application/json" -H "Accept: application/json, text/event-stream" --data-binary "@init.json"

# L9 (returns the whole log), L10 (404 instead of 405)
curl.exe -s "$url/api/events?limit=0"
curl.exe -s -o NUL -w "%{http_code}`n" "$url/api/simulation/reset"

# L4: stack two collisions, clear the first; cleared_disruptions lists both
curl.exe -s -X POST "$url/api/incidents/inject" -H "Content-Type: application/json" -d "{}"
curl.exe -s -X POST "$url/api/incidents/inject" -H "Content-Type: application/json" -d "{}"
curl.exe -s "$url/api/incidents"
curl.exe -s -X POST -H "Content-Length: 0" "$url/api/incidents/INC-0001/clear"      # use the first id from the line above

# L7, L8: reset while an analysis is simulating (reset takes ~20 s; the run ends failed with candidates still running)
curl.exe -s -X POST "$url/api/incidents/inject" -H "Content-Type: application/json" -d "{}"
curl.exe -s -X POST "$url/api/scenarios/run" -H "Content-Type: application/json" -d "{}"    # note the run id
Start-Sleep 15
curl.exe -s -X POST -H "Content-Length: 0" -w "`n[%{http_code} %{time_total}s]`n" "$url/api/simulation/reset"
curl.exe -s "$url/api/scenarios/SCN-0001"                                           # use your run id

# D1, D2: after a reset, sample once a minute and watch delay and vehicle count
$s = curl.exe -s "$url/api/state" | ConvertFrom-Json
"{0:N2} h  delay {1:N0} s  vehicles {2}" -f ($s.sim_time / 3600), $s.metrics.mean_vehicle_delay, $s.vehicles.Count

# P1: is the script compressed?  (use the file named in index.html; stale build: no content-encoding)
curl.exe -s -D - -o NUL -H "Accept-Encoding: gzip" "$url/assets/<script-from-index.html>.js"

# P4: 8 seconds of the state stream (curl exits 28 on the timeout; the file is ~9.8 MB at 16x)
curl.exe -sS --max-time 8 -o ws.out "wss://traffic-ops-demo-424216851088.us-central1.run.app/ws/state"
```

## 9. Side effects on the shared instance

I injected collisions, dispatched EMS, ran four analyses (one implemented, one aborted by a reset, one left running while I cleared its scene), applied one plan, cleared scenes, reset the city several times (once during an analysis) and switched maps between the grid and Oakland. Another person reset or switched maps around 08:03Z, and 1-2 WebSocket clients were connected throughout, so they may have seen this. Memory, analyst selection and speed were not changed.

**Final state:** Oakland, running at 16x, about 113 vehicles, no incidents, fresh ops log, `armed: null`.

## 10. Not tested

- Any visual or interactive behavior in a browser: layout at different sizes, map rendering, hover and click flows, console errors, the responsive drawer, touch.
- The autonomous episode (Arm, Analyze, Nemotron or Claude), warm recall, `double-crash`, the Analyze gate and the thinking overlay. None exist on the deployed build, and running an episode would spend Nemotron credits.
- The 3x3 grid's long-run drift (only Oakland was sampled) and any 4x run.
- Whether a browser really gets the smaller WebSocket payload with `permessage-deflate`.

## Suggested order

1. Redeploy from the branch, then re-run section 8 against it. Expect the env-var-driven values in section 0 to change.
2. Decide how the always-on twin should behave (a longer or looping demand, an automatic reset, or pausing while nobody is connected). D1, D2 and L14 all come from that one choice.
3. D3: allow the public host in the MCP transport-security settings, or remove the Cloud Run `/mcp` claim.
4. Restore `tabIndex` on the splitters (F5); key the maps on `network.id` (F1); fix F2, F3 and F4.
5. Recommendation rubric (L1, L2), Clear scene by incident (L4), and mark unfinished candidates when a run fails (L7).
6. Compression (P1) and slimmer state frames (P4).
7. The Low items, as time allows.

## Appendix A: the twin after a reset (mean delay and load, every ~2 minutes)

Samples taken once a minute from `/api/state` after a reset, 16x, Oakland, no incident. Two samplers ran back to back; the twin was reset at ~08:03Z by someone else after 7.6 h, so no sample covers 7.6-24 h.

| Sim hours | Mean delay (s) | Mean speed (m/s) | Vehicles | Stopped (< 0.1 m/s) | Waiting to enter | Segments at congestion >= 0.75 |
|---|---|---|---|---|---|---|
| 0.29 | 34 | 8.4 | 140 | 22 | 0 | 1 |
| 0.82 | 71 | 7.8 | 156 | 32 | 0 | 3 |
| 1.36 | 40 | 7.7 | 128 | 27 | 0 | 1 |
| 1.89 | 46 | 7.4 | 164 | 24 | 0 | 1 |
| 2.43 | 44 | 8.8 | 150 | 11 | 0 | 1 |
| 2.70 | 100 | 6.6 | 170 | 39 | 0 | 2 |
| 2.97 | 120 | 5.8 | 199 | 51 | 0 | 4 |
| 3.32 | 142 | 5.7 | 213 | 60 | 0 | 3 |
| 3.86 | 287 | 5.0 | 236 | 93 | 0 | 5 |
| 4.40 | 505 | 3.6 | 363 | 172 | 0 | 13 |
| 4.93 | 630 | 3.2 | 385 | 218 | 0 | 15 |
| 5.47 | 782 | 3.0 | 381 | 223 | 0 | 15 |
| 6.01 | 904 | 2.9 | 355 | 211 | 0 | 12 |
| 6.54 | 843 | 3.0 | 362 | 211 | 0 | 13 |
| 7.08 | 826 | 3.1 | 456 | 240 | 0 | 17 |
| 7.35 | 1,001 | 2.3 | 435 | 279 | 16 | 17 |
| 7.61 | 1,209 | 2.8 | 393 | 195 | 46 | 16 |

Other observations of the same instance: 5.66 h, delay 933 s (368 vehicles, 128 stopped); 8.9 h, delay 753 s (286 vehicles); 39.8 h, zero vehicles.

## Appendix B: two analyses on the deployed container

Mock analyst, 600 s horizon, incident on Forbes Avenue EB (`714429624#0`, lanes 0 and 1 of 3), EMS-1 dispatched by hand.

**Jammed twin** (SCN-0001, snapshot at sim 33,154.5, 07:30Z, 117 s wall). Recommended `corridor-plus-divert`: delay 156 -> 152 s (-2%), max queue 55 -> 48, throughput 2,148 -> 2,166. EMS response was null in every branch; the baseline probe was "stopped for 586s on 294294299#11, up to 6 vehicles ahead".

| Plan | Status | Wall (s) | Delay (s) | Max queue | Throughput (veh/h) |
|---|---|---|---|---|---|
| baseline | completed | 54.0 | 155.6 | 55 | 2,148 |
| meter-upstream | completed | 55.0 | 154.1 | 55 | 2,124 |
| relieve-cross-street | completed | 55.0 | 155.1 | 53 | 2,130 |
| aggressive-flush | rejected (`105013345 phase 0: green 8s < 12s`) | - | - | - | - |
| ems-corridor | completed | 82.6 | 153.1 | 54 | 2,154 |
| corridor-plus-divert | completed | 63.1 | 151.8 | 48 | 2,166 |
| corridor-plus-meter | completed | 62.2 | 152.2 | 54 | 2,190 |
| divert-advisory | completed | 48.7 | 153.5 | 53 | 2,154 |

**Clean twin** (after a restart, snapshot at sim 1,147.5, 10:36Z, 110 s wall). Recommended `corridor-plus-divert`: EMS 0:54 -> 0:54 (+1%), delay 115 -> 109 s (-5%), max queue 51 -> 54, throughput 2,430 -> 2,448; "divert-advisory has the same delay but ranks lower on tie-breaks".

| Plan | Status | Wall (s) | Delay (s) | Max queue | Throughput (veh/h) | EMS (s) | Notes |
|---|---|---|---|---|---|---|---|
| baseline | completed | 51.7 | 114.9 | 51 | 2,430 | 54 | EMS-1 stopped 26 s, up to 20 vehicles ahead |
| meter-upstream | completed | 52.5 | 113.3 | 46 | 2,454 | 48 | |
| relieve-cross-street | completed | 51.7 | 114.1 | 42 | 2,418 | 48 | |
| aggressive-flush | rejected | - | - | - | - | - | |
| ems-corridor | completed | 69.3 | 114.9 | 51 | 2,430 | 54 | no pre-emptions |
| corridor-plus-divert | completed | 58.1 | 109.3 | 54 | 2,448 | 54 | no pre-emptions; 31 diverted |
| corridor-plus-meter | completed | 56.0 | 113.3 | 46 | 2,454 | 48 | no pre-emptions |
| divert-advisory | completed | 46.4 | 109.3 | 54 | 2,448 | 54 | 31 diverted |

Reading it: the corridor never pre-empted a signal, so `corridor-plus-divert` and `divert-advisory` are the same result, and the plans that helped EMS (48 s) and queue (42-46) were passed over for a delay difference of 4-5 s.
